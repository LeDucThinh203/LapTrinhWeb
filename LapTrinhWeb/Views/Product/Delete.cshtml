@model LapTrinhWeb.Models.Entities.Product

@{
    ViewData["Title"] = "Xóa sản phẩm";
}

<div class="container my-5">
    <h1 class="text-danger">Xác nhận xóa sản phẩm</h1>
      <div class="alert alert-warning delete-warning">
        <h4 class="alert-heading"><i class="bi bi-exclamation-triangle"></i> Cảnh báo!</h4>
        <p>Bạn có chắc chắn muốn xóa sản phẩm này? Hành động này không thể hoàn tác.</p>
    </div>
    
    <div class="row">
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Thông tin sản phẩm sẽ bị xóa</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4 text-center">
                            <img src="@Model.Image" alt="@Model.Name" class="product-detail-image mb-3" />
                        </div>
                        <div class="col-md-8">
                            <dl class="row">
                                <dt class="col-sm-4">ID sản phẩm:</dt>
                                <dd class="col-sm-8">@Model.ProductId</dd>
                                
                                <dt class="col-sm-4">Tên sản phẩm:</dt>
                                <dd class="col-sm-8">@Model.Name</dd>
                                
                                <dt class="col-sm-4">Giá bán:</dt>
                                <dd class="col-sm-8">@Model.Price.ToString("N0") VNĐ</dd>
                                
                                <dt class="col-sm-4">Danh mục:</dt>
                                <dd class="col-sm-8">@(Model.Category?.Name ?? "Không có danh mục")</dd>
                                
                                <dt class="col-sm-4">Sản phẩm nổi bật:</dt>
                                <dd class="col-sm-8">
                                    @if (Model.IsFeatured)
                                    {
                                        <span class="badge bg-success">Có</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Không</span>
                                    }
                                </dd>
                            </dl>
                        </div>
                    </div>
                    
                    <div class="mt-3">
                        <h6>Mô tả sản phẩm:</h6>
                        <p>@Model.Description</p>
                    </div>
                </div>
                <div class="card-footer">
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="ProductId" />
                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại danh sách
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Xác nhận xóa
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">Lưu ý</h5>
                </div>
                <div class="card-body">
                    <p><strong>Dữ liệu sẽ bị xóa vĩnh viễn bao gồm:</strong></p>
                    <ul>
                        <li>Thông tin sản phẩm trong cơ sở dữ liệu</li>
                        <li>Hình ảnh sản phẩm trên máy chủ</li>
                    </ul>
                    <p><strong>Không thể xóa sản phẩm nếu:</strong></p>
                    <ul>
                        <li>Sản phẩm đã có trong đơn hàng của khách hàng</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
